# Development/test dependencies
# Note: Install in the same environment where production deps are already installed,
# or inside the Docker container where requirements.txt deps are present.
pytest==9.1.1
httpx==0.28.1
ruff==0.15.21
